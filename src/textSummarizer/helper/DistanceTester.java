package textSummarizer.helper;


import net.didion.jwnl.data.*;
import net.didion.jwnl.data.relationship.*;

public class DistanceTester {
/*	public static void main(String args[]) throws Exception
	{
		long t1 = System.currentTimeMillis();
		WordNetHelper.initialize("other/chseg/file_properties.xml");
		String ws1 = "well";
		String ws2 = "good";
		IndexWord word1 = WordNetHelper.getWord(POS.NOUN,ws1);
		IndexWord word2 = WordNetHelper.getWord(POS.NOUN, ws2);
		long t2 = System.currentTimeMillis();
		Relationship rel = WordNetHelper.getShortestPath(word1, word2);
		long t3 = System.currentTimeMillis();
		System.out.println(rel.getDepth());
		Synset lca = WordNetHelper.getLCA(rel);
		String lcaString = lca.getWord(0).getLemma();
		long nr = WordCounter.getCount(lcaString);
		long nr1 = WordCounter.getCount(ws1);
		long nr2 = WordCounter.getCount(ws2);
		double dist = Distance.getDistance(nr, nr1, nr2,2);
		System.out.println(dist);
		long t4 = System.currentTimeMillis();
		System.out.println("JC = "+(t4-t1));
		System.out.println("SP = "+(t3-t2));
	}
*/
}
